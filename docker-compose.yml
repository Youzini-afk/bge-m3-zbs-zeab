services:
  model-gateway:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      MODEL_API_KEY: ${MODEL_API_KEY:?MODEL_API_KEY is required}
      PORT: ${PORT:-8080}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-BAAI/bge-m3}
      EMBEDDING_BATCH_SIZE: ${EMBEDDING_BATCH_SIZE:-16}
      EMBEDDING_THREADS: ${EMBEDDING_THREADS:-6}
      EMBEDDING_ENGINE: ${EMBEDDING_ENGINE:-torch}
      RERANK_BASE_MODEL: ${RERANK_BASE_MODEL:-BAAI/bge-reranker-base}
      RERANK_BASE_BATCH_SIZE: ${RERANK_BASE_BATCH_SIZE:-8}
      RERANK_BASE_THREADS: ${RERANK_BASE_THREADS:-3}
      RERANK_BASE_ENGINE: ${RERANK_BASE_ENGINE:-torch}
      RERANK_V2_MODEL: ${RERANK_V2_MODEL:-BAAI/bge-reranker-v2-m3}
      RERANK_V2_BATCH_SIZE: ${RERANK_V2_BATCH_SIZE:-4}
      RERANK_V2_THREADS: ${RERANK_V2_THREADS:-4}
      RERANK_V2_ENGINE: ${RERANK_V2_ENGINE:-torch}
      HF_HOME: /app/.cache/huggingface
      TRANSFORMERS_CACHE: /app/.cache/huggingface
      HF_HUB_ENABLE_HF_TRANSFER: "0"
      TOKENIZERS_PARALLELISM: "false"
    volumes:
      - model-cache:/app/.cache/huggingface
    deploy:
      resources:
        limits:
          cpus: "${MODEL_CPUS:-10}"
          memory: ${MODEL_MEMORY:-14G}
    restart: unless-stopped

volumes:
  model-cache:
